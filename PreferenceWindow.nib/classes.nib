{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {displayPane = id; showWindow = id; }; 
            CLASS = PrefController; 
            LANGUAGE = ObjC; 
            OUTLETS = {loadingView = NSView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}