{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancel = id; ok = id; showPanel = id; }; 
            CLASS = QuickOpenPanel; 
            LANGUAGE = ObjC; 
            OUTLETS = {cancelButton = NSButton; okButton = NSButton; pathTextField = NSTextField; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}